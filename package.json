{
  "name": "message-api-test",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "John Replogle",
  "license": "ISC",
  "description": "Test API to manage messages for my github pages site",
  "dependencies": {
    "express": "^5.1.0"
  }
}
